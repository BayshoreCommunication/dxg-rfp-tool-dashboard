/**
 * Step3 is now merged into Step2RoombyRoomAVNeeds.
 * Each room renders its AV needs + production support together.
 * This stub accepts and ignores all props to avoid breaking existing call sites.
 */
export default function Step3ProductionSupportCrew(_props?: any) {
  return null;
}
